/**
 * Conflicts route — /(app)/activity/conflicts (US-007). Owner-only.
 */

import ConflictsScreen from '@modules/audit/screens/ConflictsScreen'

export default function ConflictsRoute() {
  return <ConflictsScreen />
}
