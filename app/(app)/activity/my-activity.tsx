/**
 * My Activity route — /(app)/activity/my-activity (US-007). Owner + Staff (self-scoped).
 */

import MyActivityScreen from '@modules/audit/screens/MyActivityScreen'

export default function MyActivityRoute() {
  return <MyActivityScreen />
}
