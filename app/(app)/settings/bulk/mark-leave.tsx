/**
 * Bulk Mark Leave route — /(app)/settings/bulk/mark-leave (US-011, S3).
 * Thin wrapper rendering BulkMarkLeaveScreen.
 */

import BulkMarkLeaveScreen from '@modules/settings/screens/BulkMarkLeaveScreen'

export default function BulkMarkLeaveRoute() {
  return <BulkMarkLeaveScreen />
}
